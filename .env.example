# URL base de la API de credito. Sin barra final.
# Local con Docker Compose (backend/docker-compose.yml): http://localhost:8000
# Produccion (Vercel): la URL publica de Cloud Run, p. ej. https://roda-credit-api-xxxxx.run.app
# En Vercel se configura como Environment Variable de Production; no se versiona aqui.
VITE_API_URL=http://localhost:8000
